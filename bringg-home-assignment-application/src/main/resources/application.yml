spring:
  profiles.active: 'dev'
  application:
    name: bringg-home-assignment

server:
  port: 8501
  compression:
    enabled: true
    mime-types: application/json

logging:
  file: logs/bringg-home-assignment.log
  level:
    root: 'INFO'

